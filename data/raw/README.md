# Raw data

Place raw/source data here when redistribution is allowed.

If raw data cannot be redistributed directly, provide accession IDs, download links, and reconstruction instructions in `DATA.md`.

The final benchmark uses `data/final/` and fixed folds from `data/folds/`.
