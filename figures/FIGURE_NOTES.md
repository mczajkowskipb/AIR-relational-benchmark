# Figure notes

`Fig4_method_family_balanced_accuracy` groups methods by model endpoint and representation family. The grouping is descriptive and is not intended as a calibrated interpretability score.

TSPDT/BigTSP at f200 requires `Rscript --max-ppsize=500000`.
